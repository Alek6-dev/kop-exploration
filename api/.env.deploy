###> symfony/framework-bundle ###
APP_ENV=test
APP_DEBUG=1
APP_SECRET=IciCEstUneCleLieeAGitlabCIDoncGlobalementOSEF
###< symfony/framework-bundle ###
# the server environment, could be dev, staging, prod... Used for mails
ENVIRONMENT=test

COMPOSE_PROJECT_NAME=${CI_JOB_ID}${SITE_NAME}
## For deployment, we NEED to add -T to docker-exec
DOCKEREXEC_ARG=-T
